import controller from './alerter.controller';
import template from './alerter.html'

const AlerterComponent = {
  restrict: 'E',
  bindings: {},
  template,
  controller
};

export default AlerterComponent;
